# RevCore UI Sample

Minimal sample scripts for `OptimizedScrollItem` and `PanelRoot`.

- `SampleScrollItem` updates a TMP label from `OptimizedScrollItem.Index`.
- `SamplePanelRoot` exposes `ShowSamplePanel()` for button-driven panel display.
